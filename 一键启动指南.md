# 🚀 一键启动指南

## 当前状态
✅ Redis 已运行（端口 6379）  
✅ MySQL 已运行  
⚠️ 前端需要重启  
❌ 后端未启动

---

## 立即启动步骤

### 第1步：停止前端并重启

在前端终端（正在运行 npm run dev 的终端）：
1. 按 `Ctrl+C` 停止当前服务
2. 重新启动：
```bash
npm run dev
```

**预期结果：**
- 启动在 `http://localhost:5175/`
- 没有报错信息

---

### 第2步：启动后端（新终端窗口）

**重要提示：** 后端还没有启动！404 错误就是因为后端没运行。

打开**新终端窗口**，执行：

```bash
# 1. 进入后端目录
cd "/Users/cs/Desktop/CS/创业/相亲项目开发/幸福力项目开发/backend"

# 2. 检查是否有 .env 文件
ls -la .env

# 如果没有 .env 文件，创建它：
cat > .env << 'EOF'
NODE_ENV=development
PORT=3000
API_PREFIX=api/v1

# 数据库配置（请替换成您的MySQL密码）
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=
DB_DATABASE=xingfuli

# Redis配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT配置
JWT_SECRET=xingfuli-secret-key-for-development-2024
JWT_EXPIRES_IN=7d

# COS配置（暂时留空）
COS_SECRET_ID=
COS_SECRET_KEY=
COS_BUCKET=
COS_REGION=ap-guangzhou
EOF

# 如果MySQL有密码，用文本编辑器修改 .env 文件：
# nano .env
# 找到 DB_PASSWORD= 这行，输入您的密码

# 3. 启动后端
npm run start:dev
```

**预期看到：**
```
✅ Redis连接成功（ID生成器）
✅ Redis连接成功
🚀 Application is running on: http://localhost:3000/api/v1
```

---

### 第3步：验证启动成功

#### 验证后端（在浏览器访问）：
```
http://localhost:3000/api/v1/health
```

**应该看到：**
```json
{
  "status": "ok",
  "timestamp": "2026-01-12T...",
  "service": "幸福力婚恋系统"
}
```

#### 验证前端（在浏览器访问）：
```
http://localhost:5175/
```

**应该看到：**
- 紫色渐变背景
- "幸福力婚恋系统"标题
- "进入个人中心"按钮

---

## ⚠️ 如果后端启动报错

### 错误1：数据库连接失败
```
Error: Access denied for user 'root'@'localhost'
```

**解决：**
检查 `.env` 文件中的 `DB_PASSWORD` 是否正确

### 错误2：数据库不存在
```
Error: Unknown database 'xingfuli'
```

**解决：**
需要先初始化数据库。打开新终端执行：
```bash
export PATH="/usr/local/mysql/bin:$PATH"
cd "/Users/cs/Desktop/CS/创业/相亲项目开发/幸福力项目开发/database"

mysql -u root -p < 01_create_database.sql
mysql -u root -p < 02_create_tables.sql
mysql -u root -p < 03_init_data.sql
```

然后回到后端终端，重新启动：
```bash
npm run start:dev
```

### 错误3：Redis连接失败
```
Error: connect ECONNREFUSED 127.0.0.1:6379
```

**解决：**
Redis没有运行，在新终端启动：
```bash
redis-server
```

---

## 📋 完整的终端布局

您应该有**4个终端窗口**：

**终端1 - Redis**
```bash
redis-server
# 显示：Ready to accept connections tcp
```

**终端2 - MySQL（可选，用于查询）**
```bash
export PATH="/usr/local/mysql/bin:$PATH"
mysql -u root -p
```

**终端3 - 后端**
```bash
cd "/Users/cs/Desktop/CS/创业/相亲项目开发/幸福力项目开发/backend"
npm run start:dev
# 显示：🚀 Application is running on: http://localhost:3000/api/v1
```

**终端4 - 前端**
```bash
cd "/Users/cs/Desktop/CS/创业/相亲项目开发/幸福力项目开发/frontend-h5"
npm run dev
# 显示：Local: http://localhost:5175/
```

---

## ✅ 启动成功检查清单

- [ ] Redis 显示 "Ready to accept connections tcp"
- [ ] 后端显示 "Application is running on: http://localhost:3000/api/v1"
- [ ] 前端显示 "Local: http://localhost:5175/"
- [ ] 浏览器访问 http://localhost:3000/api/v1/health 返回 JSON
- [ ] 浏览器访问 http://localhost:5175/ 显示首页

---

## 🎯 下一步：测试功能

全部启动成功后，您可以：

1. **注册新用户**：http://localhost:5175/register
2. **登录**：http://localhost:5175/login
3. **查看个人中心**：http://localhost:5175/profile

---

## 💡 提示

- 如果5175端口也被占用，Vite会自动尝试5176等其他端口
- 后端必须先启动，前端才能正常访问API
- 所有终端窗口需要保持打开状态
- 修改代码后，前端和后端会自动重新加载

---

**遇到问题？** 
请截图终端错误信息，我会帮您解决！

