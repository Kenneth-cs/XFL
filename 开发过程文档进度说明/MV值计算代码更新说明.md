# MV值计算代码更新说明

## 📅 更新时间
2026-01-21

## 📋 更新概述
根据更新后的文档 `匹配相关/MV值分地域计算方案及结果计算说明.md`，完整重写了MV值计算规则代码，确保与文档中的5套地域方案完全一致。

---

## 🔧 更新的文件

### 1. `backend/src/constants/mv-rules.constant.ts`
**状态**：✅ 完全重写

**主要变更**：
- 重新定义了5套地域方案的完整评分标准
- 新增女性 `HEIGHT` 维度（之前缺失）
- 修正了所有地域方案的男性和女性身高标准
- 调整了方案ID顺序以匹配业务逻辑

**方案ID映射表**：
| ID | 方案名称 | Code |
|----|---------|------|
| 1 | 上海北京地区 | BEIJING_SHANGHAI |
| 2 | 东北新疆地区 | NORTHEAST_XINJIANG |
| 3 | 广东周边地区 | GUANGDONG |
| 4 | 江浙周边地区 | JIANGZHE |
| 5 | 全国普适版 | NATIONWIDE |

### 2. `backend/src/modules/mv/mv.service.ts`
**状态**：✅ 已更新

**主要变更**：
- 在 `calculateFemaleMv` 方法中新增 `height` 维度的计算
- 确保女性MV值计算包含所有8个维度

---

## 📊 关键修正点

### 男性身高标准修正

| 地域方案 | 旧标准（错误） | 新标准（正确） | 变化 |
|---------|--------------|--------------|------|
| 上海北京 | 未单独定义 | 175以下=8.5, 175-179=10.5, **180-184=12.5**, 184以上=10.5 | ✅ 新增 |
| 东北新疆 | 180-185=12.5 | 175-180=10.5, **181-185=12.5**, 185以上=10.5 | ✅ 修正 |
| 广东周边 | 170-175=10.5 | **172以下=8.5**, 172-176=10.5, **177-181=12.5**, 181以上=10.5 | ✅ 修正 |
| 江浙周边 | 未单独定义 | **173以下=8.5**, 173-177=10.5, **178-182=12.5**, 182以上=10.5 | ✅ 新增 |
| 全国普适 | 170-175=10.5 | 170以下=8.5, **171-175=10.5**, **176-180=12.5**, 180以上=10.5 | ✅ 修正 |

### 女性身高标准新增

所有5个地域方案的女性都新增了 `HEIGHT` 维度：

| 地域方案 | 身高标准 | 状态 |
|---------|---------|------|
| 上海北京 | 158以下=8.5, 159-163=10.5, 164-168=12.5, 169-173=10.5, 174-179=9, 180以上=8 | ✅ 新增 |
| 东北新疆 | 157以下=8.5, 158-162=10.5, 163-167=12.5, 168-172=10.5, 173-178=9, 179以上=8 | ✅ 新增 |
| 广东周边 | 152以下=8.5, 153-157=10.5, 158-162=12.5, 163-167=10.5, 168-172=9, 172以上=8 | ✅ 新增 |
| 江浙周边 | 155以下=8.5, 156-160=10.5, 161-165=12.5, 166-170=10.5, 171-177=9, 177以上=8 | ✅ 新增 |
| 全国普适 | 152以下=8.5, 153-157=10.5, 158-162=12.5, 163-167=10.5, 168-172=9, 172以上=8 | ✅ 新增 |

### 女性BMI标准修正

所有方案统一修正为：
- **旧标准**：`24-32=8.5, >32=6.5`
- **新标准**：`24-27.9=8.5, ≥28.0=6.5` ✅

---

## 🎯 完整的维度清单

### 男性MV值计算维度（8项）
1. **年龄** (AGE)
2. **身高** (HEIGHT)
3. **长相** (APPEARANCE)
4. **财富** (WEALTH)
5. **智商/学历** (INTELLIGENCE)
6. **情商** (EQ)
7. **性能力** (SEXUAL_ABILITY) - 根据年龄自动计算
8. **长期专一承诺** (COMMITMENT) - 根据婚恋幸福力评测结果

### 女性MV值计算维度（8项）
1. **年龄** (AGE)
2. **身高** (HEIGHT) ⭐ 新增
3. **BMI** (BMI) - 根据身高体重自动计算
4. **长相** (APPEARANCE)
5. **罩杯** (BRA_CUP)
6. **学历** (EDUCATION)
7. **性格** (PERSONALITY) - 根据九型人格测评自动计算
8. **家庭环境** (FAMILY)

---

## ⚠️ 重要变更说明

### 1. 女性维度数量变化
- **之前**：7个维度
- **现在**：8个维度（新增身高）
- **影响**：女性MV值总分计算范围变化，可能导致历史数据需要重新计算

### 2. 身高标准地域化差异
不同地域对身高的要求不同，充分体现地域特色：
- **东北新疆**：标准最高（男181-185cm最优，女163-167cm最优）
- **江浙周边**：标准较高（男178-182cm最优，女161-165cm最优）
- **广东周边**：标准适中（男177-181cm最优，女158-162cm最优）
- **上海北京**：标准较高（男180-184cm最优，女164-168cm最优）
- **全国普适**：标准较宽松（男176-180cm最优，女158-162cm最优）

### 3. 方案ID重新排序
为了业务逻辑清晰，重新调整了方案ID：
- ID 1: 上海北京（一线城市，标准高）
- ID 2: 东北新疆（身高标准最高）
- ID 3: 广东周边（经济发达）
- ID 4: 江浙周边（经济发达）
- ID 5: 全国普适（兜底方案）

**⚠️ 注意**：如果数据库中已有门店绑定了旧的方案ID，需要进行数据迁移！

---

## 🧪 测试建议

### 测试用例 1：女性身高评分
**场景**：广东周边地区，女性用户
- 身高 158cm → 期望得分：12.5分（最优区间）
- 身高 165cm → 期望得分：10.5分
- 身高 172cm → 期望得分：9分

### 测试用例 2：男性身高评分
**场景**：东北新疆地区，男性用户
- 身高 183cm → 期望得分：12.5分（181-185cm最优）
- 身高 175cm → 期望得分：10.5分
- 身高 170cm → 期望得分：8.5分

### 测试用例 3：女性BMI评分
**场景**：任意地域，女性用户
- BMI 22 → 期望得分：12.5分（正常）
- BMI 27.5 → 期望得分：8.5分（超重）
- BMI 28.5 → 期望得分：6.5分（≥28.0）

### 测试用例 4：完整MV值计算
**场景**：上海北京地区，女性25岁
- 身高：165cm
- 体重：55kg
- 学历：985或更高
- 长相：漂亮/网红
- 罩杯：C
- 性格：匹配到5种异性
- 家庭：父母恩爱

**期望结果**：
- 年龄：12.5
- 身高：10.5
- BMI：12.5
- 长相：12.5
- 罩杯：12.5
- 学历：12.5
- 性格：12.5
- 家庭：12.5
- **总分**：98.0

---

## 📝 数据迁移建议

### 1. 方案ID映射（如果需要）
如果现有数据库中的方案ID与新代码不一致，需要执行：

```sql
-- 示例：将旧的方案ID 1（广东）映射为新的ID 3
UPDATE sys_store SET mv_template_id = 3 WHERE mv_template_id = 1;
```

### 2. 重新计算所有用户MV值
由于女性新增了身高维度，建议重新计算所有女性用户的MV值：

```typescript
// 伪代码
const femaleUsers = await findAllFemaleUsers();
for (const user of femaleUsers) {
  await mvService.calculateMv(user.id);
}
```

---

## ✅ 更新完成检查清单

- [x] 更新 `backend/src/constants/mv-rules.constant.ts`
- [x] 新增女性 HEIGHT 维度到所有5个方案
- [x] 修正所有地域的男性身高标准
- [x] 修正所有地域的女性BMI标准
- [x] 更新 `backend/src/modules/mv/mv.service.ts`
- [x] 在女性MV计算中添加身高维度
- [x] **补充修复**：长相字段匹配规则（支持"清秀"、"甜美"、"可爱"、"帅气"等）
- [x] **补充修复**：家庭环境字段匹配规则（支持"和睦"关键字）
- [x] **补充修复**：性格字段匹配规则（明确"未测评"状态）
- [ ] **待完成**：重启后端服务
- [ ] **待完成**：测试MV计算功能
- [ ] **待完成**：重新计算历史用户MV值（可选）

---

## 🔧 补充修复（2026-01-21 追加）

### 问题：字段值匹配规则不完善

**症状**：MV计算结果中出现"无匹配规则"

**原因**：
1. 长相字段只匹配固定值（"一般"、"帅/网红"），但实际数据有多种表述
2. 家庭环境字段匹配条件过于严格
3. 性格字段缺少"未测评"状态的处理

**修复方案**：

#### 1. 女性长相（APPEARANCE）
```typescript
// 修复前
{ condition: (a) => a === '一般', score: 8.5, label: '一般' },
{ condition: (a) => a === '漂亮/网红', score: 12.5, label: '漂亮/网红' }

// 修复后
{ condition: (a) => a === '一般', score: 8.5, label: '一般' },
{ condition: (a) => a === '清秀' || a === '甜美' || a === '可爱', score: 10.5, label: '清秀/甜美' },
{ condition: (a) => a === '漂亮/网红', score: 12.5, label: '漂亮/网红' }
```

#### 2. 男性长相（APPEARANCE）
```typescript
// 修复前
{ condition: (a) => a === '一般', score: 8.5, label: '一般' },
{ condition: (a) => a === '帅/网红', score: 12.5, label: '帅/网红' }

// 修复后
{ condition: (a) => a === '一般', score: 8.5, label: '一般' },
{ condition: (a) => a === '帅气' || a === '阳光' || a === '斯文', score: 10.5, label: '帅气' },
{ condition: (a) => a === '帅/网红' || a === '阳光帅气', score: 12.5, label: '帅/网红' }
```

#### 3. 家庭环境（FAMILY）
```typescript
// 修复前
{ condition: (f) => f === '父母恩爱，相互尊重（家庭有爱）' || f?.includes('恩爱'), score: 12.5, ... }

// 修复后
{ condition: (f) => f?.includes('恩爱') || f?.includes('和睦') || f?.includes('有爱'), score: 12.5, ... }
```

#### 4. 性格匹配度（PERSONALITY）
```typescript
// 新增：未测评状态
{ condition: (p) => p === 0 || p === null || p === undefined, score: 0, label: '未测评' }
```

**效果**：
- ✅ "清秀" → 10.5分（清秀/甜美）
- ✅ "父母健康（家庭和睦）" → 12.5分
- ✅ 未进行九型人格测评 → 0分（未测评）

---

## 🚀 部署建议

### 本地测试
```bash
# 1. 重启后端
cd backend
npm run start:dev

# 2. 测试MV计算接口
curl -X POST http://localhost:3000/api/v1/mv/calculate/{userId} \
  -H "Authorization: Bearer {token}"
```

### 生产环境部署
```bash
# 1. 提交代码
git add backend/src/constants/mv-rules.constant.ts backend/src/modules/mv/mv.service.ts
git commit -m "更新MV值计算规则，新增女性身高维度"

# 2. 重新构建后端
cd backend
npm run build

# 3. 重启PM2
pm2 restart xinfu-backend

# 4. 验证
pm2 logs xinfu-backend --lines 20
```

---

## 📚 参考文档
- 源文档：`匹配相关/MV值分地域计算方案及结果计算说明.md`
- 相关PRD：`项目核心文档/PRD_幸福力项目_v1.0.md`
- 架构设计：`项目核心文档/系统架构与数据库设计_幸福力项目_v1.0.md`

---

## 👤 更新人
AI Assistant

## ✍️ 审核
待用户确认
